import { BrainCircuit, Database, FileInput, Network } from 'lucide-react';
const steps = [
  { icon: FileInput, title: 'Share', text: 'Add a conversation, thought, or file.' },
  { icon: Database, title: 'Remember', text: 'Information becomes structured memory.' },
  { icon: BrainCircuit, title: 'Reason', text: 'Cognitive engines use the relevant context.' },
  { icon: Network, title: 'Evolve', text: 'Connections strengthen with every interaction.' },
];
export function HowItWorks() { return <section id="how-it-works" className="scroll-mt-20 py-20 sm:py-28" aria-labelledby="how-heading"><div className="mx-auto mb-12 max-w-2xl text-center"><p className="mb-3 text-sm font-semibold uppercase tracking-[0.18em] text-violet-600 dark:text-violet-400">How it works</p><h2 id="how-heading" className="text-3xl font-semibold tracking-tight sm:text-5xl">From information to understanding.</h2><p className="mt-4 text-lg text-muted-foreground">A simple loop turns each interaction into context for the next.</p></div><ol className="relative grid gap-5 md:grid-cols-4 before:absolute before:left-[12.5%] before:right-[12.5%] before:top-7 before:hidden before:h-px before:bg-border md:before:block">{steps.map(({icon: Icon,title,text}, index) => <li key={title} className="relative text-center"><span className="relative mx-auto grid size-14 place-items-center rounded-2xl border border-border bg-background text-violet-600 shadow-sm dark:text-violet-400"><Icon className="size-6" /></span><p className="mt-5 text-xs font-semibold uppercase tracking-wider text-muted-foreground">Step {index + 1}</p><h3 className="mt-1 text-lg font-semibold">{title}</h3><p className="mx-auto mt-2 max-w-[15rem] text-sm leading-6 text-muted-foreground">{text}</p></li>)}</ol></section>; }
